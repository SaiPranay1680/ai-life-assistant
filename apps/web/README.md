# LIFE AI web app

Next.js frontend for AI Life Assistant.

```bash
cd apps/web
cp .env.example .env.local
npm install
npm run dev
```

Open http://localhost:3000

Sign in with any valid email and a password of 6+ characters. Data is mocked until the FastAPI backend is connected.
