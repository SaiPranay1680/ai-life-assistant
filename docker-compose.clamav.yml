services:
  clamav:
    image: clamav/clamav:stable
    container_name: ai-clamav
    ports:
      - "3310:3310"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "clamdscan --ping 1 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 8
      start_period: 180s
